# TODO
- Nixos-anywhere deploy
- Agenix key generation on target server
- Agenix public key stored accessible to pipelines
- Agenix-encrypted credentials stored on target server
- Set up auto-update service
- Set up minecraft server
- Set up backup service
- Set up backup restore in case of fresh server install
- Set up backup pruning
- Set up web server
- Load player whitelist/banlist into webserver on startup
- Set up OAuth with Feide for login
- Set up jwt generation with mod/admin roles
- Set up Redis for refresh tokens and caching of player-related server files(player list)
- Set up nixos-rebuild admin access
- Set up rollback functionality through admin interface
- Set up manual mod whitelist/remove functionality
- Fetch player accound UUIDs automatically
- Set up mod ban functinality(on feide user as opposed to whitelist removal of minecraft username)
- Set up monitoring for important systems with fault notification systems
- Implement secret rotation mechanisms
- Implement a singular queue for changes to server files

# DOING

# DONE